<div class="relative h-screen bg-cover bg-center" style="background-image: url('<%= asset_path("home_background.jpg") %>');">
  <div class="absolute inset-0 bg-black/20"></div>

  <div class="relative z-10 h-full">
  <!-- Navbar Section -->
<%= render "shared/navbar" %>

<div class="min-h-screen flex flex-col items-center w-full">

  <!-- Hero Section -->
  <section class="w-full max-w-2xl mt-40 p-6 flex flex-col items-center">

    <h1 class="text-3xl md:text-8xl font-thin text-white mb-20 text-center">Aqilah & Aaron</h1>
    <p class="text-lg font-thin text-white mb-10 text-center">We are getting married! <br> <strong>November 17, 2025</strong> - Singapur</p>
    <!-- Countdown Timer -->
    <div id="countdown" class="flex space-x-4 text-center mb-4">
      <div>
        <span id="days" class="block text-2xl md:text-3xl font-bold text-white">138</span>
        <span class="text-xs text-white">Days</span>
      </div>
      <div>
        <span id="hours" class="block text-2xl md:text-3xl font-bold text-white">03</span>
        <span class="text-xs text-white">Hours</span>
      </div>
      <div>
        <span id="minutes" class="block text-2xl md:text-3xl font-bold text-white">19</span>
        <span class="text-xs text-white">Mins</span>
      </div>
      <div>
        <span id="seconds" class="block text-2xl md:text-3xl font-bold text-white">04</span>
        <span class="text-xs text-white">Secs</span>
      </div>
    </div>
  </section>

</div>

</div>

<%# detalles %>
</div>

<section id="location" class="w-full min-h-screen px-6 py-10">
  <div class="max-w-5xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-8 h-full items-center">

    <!-- Columna 1: Detalles -->
    <div class="flex flex-col justify-center">
      <h2 class="text-xl md:text-2xl font-thin mb-4">Details of the Event</h2>
      <p class="text-gray-700 mb-2"><span class="font-semibold">Date:</span> November 17th, 2025</p>
      <p class="text-gray-700 mb-2"><span class="font-semibold">Time:</span> 17:00</p>
      <p class="text-gray-700 mb-6"><span class="font-semibold">Location:</span> Singapur</p>
    </div>

    <!-- Columna 2: Puedes poner una imagen o mapa embebido -->
    <div class="flex justify-center items-center">
      <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d261929.1741848741!2d103.67943744017099!3d1.3139946156399815!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x31da11238a8b9375%3A0x887869cf52abf5c4!2sSingapore!5e1!3m2!1sen!2sde!4v1751969402584!5m2!1sen!2sde" width="600" height="450" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
    </div>

  </div>
</section>

<script>
  // Simple countdown timer (set your event date below)
  document.addEventListener("DOMContentLoaded", function() {
    const eventDate = new Date("2025-11-17T17:00:00");
    function updateCountdown() {
      const now = new Date();
      const diff = eventDate - now;
      if (diff <= 0) return;
      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
      const minutes = Math.floor((diff / (1000 * 60)) % 60);
      const seconds = Math.floor((diff / 1000) % 60);
      document.getElementById("days").textContent = String(days).padStart(2, '0');
      document.getElementById("hours").textContent = String(hours).padStart(2, '0');
      document.getElementById("minutes").textContent = String(minutes).padStart(2, '0');
      document.getElementById("seconds").textContent = String(seconds).padStart(2, '0');
    }
    setInterval(updateCountdown, 1000);
    updateCountdown();
  });
</script>
