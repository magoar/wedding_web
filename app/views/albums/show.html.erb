<%# ...existing code... %>
<div class="px-4 py-8 max-w-4xl mx-auto">
  <h1 class="text-lg sm:text-2xl font-semibold mb-2 text-gray-800">Now select the photos and videos you want to share with the rest!</h1>

 <div class="mt-4">
    <button id="cloudinary_upload_button"
            data-album-id="<%= @album.id %>"
            aria-label="Upload photos and videos"
            class="w-full sm:inline-flex sm:w-auto justify-center items-center gap-3 bg-gray-600 hover:bg-gray-700 text-white text-lg sm:text-base px-4 py-3 sm:px-3 sm:py-2 rounded-lg shadow">
      Upload Photos & Videos
    </button>
  </div>

  <div class="mt-6 flex justify-between items-center">
    <%= link_to "Done", albums_path,
        class: "inline-block bg-green-700 text-white px-4 py-2 rounded hover:bg-green-800 transition",
        data: { turbo: false },
        notice: "Album creado correctamente!" %>
  </div>
</div>
<%# ...existing code... %>
